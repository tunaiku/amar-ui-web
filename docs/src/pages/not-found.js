import React from 'react';
import { AppLayout } from '@layouts/app-layout';

const NotFoundPage = () => (
  <AppLayout>
    <h1>Oops! It looks like the page you are looking for doesn't exist!</h1>
  </AppLayout>
);

export default NotFoundPage;
