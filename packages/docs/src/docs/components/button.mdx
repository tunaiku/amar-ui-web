---
path: '/components/button'
title: 'Button'
description: 'Button is an interactive element that carries an action that returns a feedback when
clicked.'
---

## Usage

Simply add the `.Button` class in a `<button>` or `<a>` tag to apply the standard button styling.
From here, there are many options which you can choose to further modify your button.

```html preview=true
<button class="Button">Default button</button>
```

A little note for accessibility; adding `.Button` class to `<a>` is completely possible, but always
put `role="button"` attribute. This will make sure `<a>` element is recognized as a clickable
element by screen readers.

## Themes

You can apply a theme to a button to make it the primary call-to-action element of a component.
Applying a theme is required to apply variants to a button. If you choose to not apply other
variants, a themed button will hold the base emphasis that typically used for primary actions.

To apply a themed button, simply append the `.Button--<theme>` class alongside the `.Button` class.

```html preview=true
<button class="Button Button--primary">Primary</button>
<button class="Button Button--secondary">Secondary</button>
<button class="Button Button--success">Warning</button>
<button class="Button Button--warning">Warning</button>
<button class="Button Button--danger">Danger</button>
<button class="Button Button--dark">Dark</button>
<button class="Button Button--light">Light</button>
```

## Variants

We have provided

You can further modify a button with a set of handful **variants**. Variants are differentiated by
their level of **emphasis**. Please note that variants can only work in a **themed button**.

```html preview=true
<button class="Button Button--primary">Button</button>
<button class="Button Button--primary Button--outlined">Button</button>
<button class="Button Button--primary Button--text">Button</button>
<button class="Button Button--primary Button--elevated">Button</button>
```

#### Outlined button

Outlined button has a lower emphasis than a themed button and is usually used for secondary actions.

To apply an outlined button, append `.Button--outlined` alongside `Button` and `Button--<theme>`.

```html preview=true bgColor=neutral.10
<button class="Button Button--outlined Button--primary">Primary</button>
<button class="Button Button--outlined Button--secondary">Secondary</button>
<button class="Button Button--outlined Button--warning">Warning</button>
<button class="Button Button--outlined Button--danger">Danger</button>
<button class="Button Button--outlined Button--dark">Dark</button>
<button class="Button Button--outlined Button--light">Light</button>
```

#### Text button

**Text button** has no border and background fill, leaving only a clickable text. It has the
**lowest emphasis** than any other variants.

To apply a text button, append `.Button--text` alongside `Button` and `Button--<theme>`.

```html preview=true
<button class="Button Button--text Button--primary">Primary</button>
<button class="Button Button--text Button--secondary">Secondary</button>
<button class="Button Button--text Button--warning">Warning</button>
<button class="Button Button--text Button--danger">Danger</button>
<button class="Button Button--text Button--dark">Dark</button>
<button class="Button Button--text Button--light">Light</button>
```

#### Elevated button

Elevated button has a custom effect to make your button appear floated, similar to Material Design's
button.

```html preview=true
<button class="Button Button--primary Button--elevated">Primary</button>
<button class="Button Button--secondary Button--elevated">Secondary</button>
<button class="Button Button--danger Button--elevated">Danger</button>
<button class="Button Button--warning Button--elevated">Warning</button>
<button class="Button Button--success Button--elevated">Success</button>
<button class="Button Button--dark Button--elevated">Dark</button>
<button class="Button Button--light Button--elevated">Light</button>
```

## Modifiers

Modifiers differ than variants in that they are freely mix-and-matched with each other to form
different button entities.

#### Sizes

Append `Button--<size>` modifier class to change the size of a button. Out of the box, there are
three different sizes you can choose from.

```html preview=true
<button class="Button Button--primary Button--small">Small</button>
<button class="Button Button--primary">Base</button>
<button class="Button Button--primary Button--large">Large</button>
```

#### Rounded

Append `Button--rounded` modifier to turn buttons into a pill-like shape.

```html preview=true
<button class="Button Button--primary Button--rounded">A rounded button</button>
<button class="Button Button--secondary Button--outlined Button--rounded">A rounded button</button>
```

#### Full Width

Append the `Button--fullWidth` modifier to increase a button's width to expand over its parent's
width.

```html preview=true
<button className="Button Button--primary Button--fullWidth MarginBottom">
  Primary Button
</button>
<button className="Button Button--secondary Button--outlined Button--fullWidth MarginBottom">
  Outlined Button
</button>
```

#### Disabled

Turns a button into disabled state which makes it **noninteractive**.

```html preview=true
<button className="Button Button--primary" disabled>Disabled button</button>
<a className="Button Button--primary is-disabled">Disabled button</a>
```
